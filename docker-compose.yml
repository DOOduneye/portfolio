services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db-data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/site.db
    restart: unless-stopped

volumes:
  db-data:
